In the sub-directories:

1. Design variables of the optimum/sample are in the file <DesVar.dat>.
2. Objecte and constraints are in the file <Cost.dat>. The first colume is the objective, the second and third columes are values of constraint functions, which satisfy the constraints when they are negative.
3. File <results.out.Nr> keeps the results of structure analysis.
